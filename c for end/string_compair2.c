#include<stdio.h>
#include<string.h>
void main()
{
    char a[22]="shivay";
    char b[33]="mahadev";
    if(strcmp(a,b)==0)
    {
        printf("string are same");
    }
    else
    {
        printf("string are not same");
    }
}